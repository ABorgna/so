#include "sched_sjf.h"
#include <iostream>
#include <queue>
#include <vector>

using namespace std;

SchedSJF::SchedSJF(vector<int> argn) {
    // Recibe la cantidad de cores
    /* llenar */
}

SchedSJF::~SchedSJF() { /* llenar */ }

void SchedSJF::load(int pid) { /* llenar */ }

void SchedSJF::unblock(int pid) { /* llenar */ }

int SchedSJF::tick(int cpu, const enum Motivo m) {
    /* llenar */
    return 0;
}
